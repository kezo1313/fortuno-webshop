<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>
        <!-- 1. FiscalizationService -->
        <service id="Fortuno\Fiscalization\Service\FiscalizationService">
            <argument type="service" id="Shopware\Core\System\SystemConfig\SystemConfigService"/>
            <argument type="service" id="order.repository"/>
            <argument type="service" id="logger"/>
            <!-- REMOVED MediaService because the API approach doesn't use it -->
        </service>

        <!-- 2. FiscalizationController -->
        <service id="Fortuno\Fiscalization\Controller\Api\FiscalizationController" public="true">
            <!-- Inject the service defined above -->
            <argument type="service" id="Fortuno\Fiscalization\Service\FiscalizationService"/>
            
            <call method="setContainer">
                <argument type="service" id="service_container"/>
            </call>
        </service>
    </services>
</container>