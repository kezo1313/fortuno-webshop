framework:
    # Proxy configuration moved to trusted_env.yaml to avoid duplication
    # trusted_headers: [ 'x-forwarded-host', 'x-forwarded-proto', 'x-forwarded-for', 'x-forwarded-port' ]
    # trusted_proxies: '%env(TRUSTED_PROXIES)%'